# AGENTS.md

## 필수 지침

- 작업을 시작하기 전에 `docs/AI_AGENT_GUIDE.md`를 끝까지 읽고 따른다. 프로젝트 아키텍처, 도메인 함정, 테스트 관례의 단일 출처다.
- 저장소를 먼저 조사한 뒤 기존 구현과 패턴을 재사용한다. 특히 사주 계산 공식을 추측하거나 중복 구현하지 않는다.
- 패키지 관리에는 pnpm을 사용하고 기존 `pnpm-lock.yaml`을 유지한다.
- 사용자 변경과 관련 없는 작업 트리 변경을 보존한다.
- 변경 범위에 맞는 테스트·타입 검사·린트를 실행하고, 실행하지 못한 검증은 완료 보고에 명시한다.

## Codex 작업 규칙

- 코드 검색에는 우선 `rg`와 `rg --files`를 사용한다.
- 수정 전 관련 소스와 콜로케이션 테스트를 함께 읽는다.
- 버그 수정에는 회귀 이유가 드러나는 테스트를 추가한다.
- 계산 결과의 결정론, 정밀 절기 경계, 시간 미상 처리, 캐시 키 입력 누락 여부를 항상 점검한다.
- 새 의존성이나 공개 인터페이스 변경은 필요성과 호환성 영향을 먼저 확인한다.
